{
  "name": "vue-memoize-dict-root",
  "private": true,
  "packageManager": "pnpm@8.6.6",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "play": "pnpm run -r play",
    "build": "pnpm run -r build",
    "clean": "pnpm run -r clean",
    "publish": "npm publish --access public vue-memoize-dict"
  },
  "stackblitz": {
    "startCommand": "pnpm play"
  }
}